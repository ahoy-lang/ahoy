? Combined test: inline conditionals + enhanced loops
import "stdio.h"

ahoy|"=== Combined Features Test ===\n\n"|

? Test 1: Inline conditionals with range loops
ahoy|"1. Checking even/odd in range:\n"|
loop:0 to 6 then
    if 0 is 0 then ahoy|"   - even\n"| else ahoy|"   - odd\n"|

? Test 2: Array iteration with inline conditionals
ahoy|"\n2. Grading scores:\n"|
scores: <95, 72, 88, 91, 65>
loop score in scores then
    if score greater_than 90 then ahoy|"   Score %d: A\n", score| anif score greater_than 80 then ahoy|"   Score %d: B\n", score| anif score greater_than 70 then ahoy|"   Score %d: C\n", score| else ahoy|"   Score %d: F\n", score|

? Test 3: While loop with inline condition check
ahoy|"\n3. Countdown with status:\n"|
timer: 5
loop:timer greater_than 0 then
    if timer greater_than 3 then ahoy|"   %d (green)\n", timer| anif timer greater_than 1 then ahoy|"   %d (yellow)\n", timer| else ahoy|"   %d (red)\n", timer|
    timer: timer minus 1

? Test 4: Dictionary iteration with inline conditionals
ahoy|"\n4. Checking settings:\n"|
config: {"mode":"production", "debug":"off", "cache":"enabled"}
loop key,value in config then
    if value is "on" then ahoy|"   ✓ %s is ON\n", key| anif value is "off" then ahoy|"   ✗ %s is OFF\n", key| else ahoy|"   • %s = %s\n", key, value|

? Test 5: Nested loops with inline conditionals
ahoy|"\n5. Grid with markers:\n"|
loop:0 to 3 then
    ahoy|"   "|
    loop:0 to 4 then
        if 0 is 0 then ahoy|"# "| else ahoy|". "|
    ahoy|" \n"|

ahoy|"\n=== All combined tests passed! ===\n"|
