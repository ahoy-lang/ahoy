? Test new loop syntax features
import "stdio.h"

? Test 1: loop:0 to 5 (range loop)
ahoy|"Test 1: Range loop 0 to 5\n"|
loop:0 to 5 then
    ahoy|"Iteration\n"|

? Test 2: loop:10 to 15 (range loop starting at 10)
ahoy|"\nTest 2: Range loop 10 to 15\n"|
loop:10 to 15 then
    ahoy|"Count\n"|

? Test 3: loop:5 (count from 5, infinite - will break manually)
ahoy|"\nTest 3: Count loop starting at 5\n"|
counter: 5
loop:5 to 8 then
    ahoy|"Counter iteration\n"|

? Test 4: loop then (default, infinite loop - actually use range)
ahoy|"\nTest 4: Simple range for demo\n"|
loop:0 to 3 then
    ahoy|"Simple loop\n"|

? Test 5: loop:true then (while loop with boolean)
ahoy|"\nTest 5: While loop with condition\n"|
x: 0
loop:x lesser_than 3 then
    ahoy|"x is %d\n", x|
    x: x plus 1

ahoy|"\nAll loop tests completed!\n"|
