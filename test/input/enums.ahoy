expected: []
? Enums test

enum:int numbers ? enum should default to int type 0,1,2,... not nillable
	one
	two
	three
$
print|numbers.one,numbers.two,numbers.three|
expected.push|0, 1, 2|
print|numbers|
expected.push|"enum:int numbers(one:0, two:1, three:2)"|
print|numbers.type|
expected.push|"enum:int"|
print|numbers.one.type|
expected.push|"int"|

enum:int explicit: ? enum with explicit default initial values
	1 one
	5 five
	10 ten
	0 zero
$
print|explicit.one,explicit.five,explicit.ten,explicit.zero|
expected.push|1, 5, 10, 0|


enum one_line_enum: 1 one, 5 five, 10 ten, $
print|one_line_enum.one,one_line_enum.five,one_line_enum.ten|
expected.push|1, 5, 10|


? string enum with immutable string values that cannot be reassigned to
enum:string names:
	default  ? should be initialized to zero value for string which is: ""
	"jared" jared
	"jacinda" jacinda
	"bob" bob
$

enum:color colors:
	default	? should be initialized to zero value for color which is: < 0, 0, 0, 0 >
	color< 230, 41, 55, 255 > red
	color< 0, 228, 48, 255 > green
	color< 0, 121, 241, 255 > blue
$
print|colors.red,colors.green,colors.blue, colors.default|
expected.push|"color<230, 41, 55, 255>, color<0, 228, 48, 255>, color<0, 121, 241, 255>,color<0, 0, 0, 0>"|
print|colors.type|
expected.push|"enum:color"|

? mixed enuma like in programming languages that support mixed enums suchas typescript
enum mixed_enum:
	1 one,
	"bob jones" name,
	3.0 three,
	[2,3,4] an_array:mutable,
$

print|mixed_enum.one,mixed_enum.name,mixed_enum.three,mixed_enum.an_array|
mixed_enum.an_array[0]:int=10
expected.push|"1, bob jones, 3.0, [10, 3, 4]"|
print|mixed_enum.type|
expected.push|"enum"|


? Print expected values for test validation
print|expected|
