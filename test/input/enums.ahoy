expected: []
? Enums test

enum:int numbers ? enum should default to int type 0,1,2,... not nillable
	one
	two
	three
$
x:int = numbers.one; y:int = numbers.two; z:int = numbers.three
print|x,y,z|
expected.push|"0 1 2"|
print|numbers|
expected.push|"enum:int numbers(one:0, two:1, three:2)"|
print|numbers.type|
expected.push|"enum:int"|
print|numbers.one.type|
expected.push|"int"|

enum:int explicit: ? enum with explicit default initial values
	1 one
	5 five
	10 ten
	0 zero
$
b:int = explicit.five
c:int = explicit.ten
d:int = explicit.zero
print|explicit.one,b,c,d|
expected.push|"1 5 10 0"|


enum one_line_enum: 1 one, 5 five, 10 ten, $
e:int = one_line_enum.one
f:int = one_line_enum.five
g: one_line_enum.ten
print|e,f,g|
expected.push|"1 5 10"|
print|e.type,f.type,g.type|
expected.push|"int int int"|


? string enum with immutable string values that cannot be reassigned to
enum:string names:
	none  ? should be initialized to zero value for string which is: ""
	"jared" jared
	"jacinda" jacinda
	"bob" bob
$
print|names.jared,names.jacinda,names.bob|
expected.push|"jared jacinda bob"|

? NOTE: Color enums temporarily disabled due to C keyword 'default' and parser complexity
? Will re-enable after fixing parser lookahead for color<...> syntax

? mixed enums like in programming languages that support mixed enums such as typescript
enum mixed_enum:
	1 one,
	"bob jones" name,
	3.0 three,
$

print|mixed_enum.one,mixed_enum.name,mixed_enum.three|
expected.push|"1 bob jones 3.000000"|
? NOTE: Array members temporarily disabled due to linter expecting object syntax
? [2,3,4] an_array:mutable,
? mixed_enum.an_array[0]:10
? expected.push|"1 bob jones 3.000000 [10, 3, 4]"|
print|mixed_enum.type|
expected.push|"enum"|


? Print expected values for test validation
print|expected|
